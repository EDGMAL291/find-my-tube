{
  "name": "find-my-tube",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html, *.css, *.js\" --no-open --port 3000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
