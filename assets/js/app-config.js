window.FMT_APP_CONFIG = Object.assign({}, window.FMT_APP_CONFIG, {
  // Optional: override the local stock API base, for example "http://localhost:3000".
  stockApiBaseUrl: "",
  // Optional: override the built-in local backend submit path.
  stockOrderSubmitUrl: ""
});
